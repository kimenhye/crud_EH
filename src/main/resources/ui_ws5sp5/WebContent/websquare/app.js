(function() {
    WebSquare.configCache["/WebSquare/wpack/contextRoot/@value"] = WebSquareExternal.contextPath;

    var promiseObj = WebSquare.startApplication();
    promiseObj.then(function(resolve, reject) {

    });
})();